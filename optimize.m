% function optimizedLab = optimize(Lab, n)
% takes in list of Lab values
% removes the Lab values that are closest to each other
% returns list of the n Lab values that are least close to each other

load('labval.mat');

dim = size(labval);

for i = 1:dim(1)
    for j = i+1:dim(1)
        
    end
end





% end